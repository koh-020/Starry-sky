//package code;
//
//public class Main {
//	static MainWindow mainWindow;
//	public static void main(String[] args) {
//		mainWindow = new MainWindow();	//ウィンドウのみを生成
//		mainWindow.preparePanels();     //ペインに直接貼るパネルのみを生成
//		mainWindow.prepareComponents(); //その他のコンポーネントを生成
//		mainWindow.setFrontScreenAndFocus(ScreenMode.TITLE); //起動後最初に表示するべき画面を手前に持ってきてそれに注目させる
//		mainWindow.setVisible(true);    //最後にウィンドウを可視化
//	}
//}





//課題
package code;
public class Main {
	public static void main(String[] args) {
		
		int i = 0;
		int loopCount = 100;
		int numFizz = 3;
		int numBuzz = 5;
		String strFizz = "Fizz";
		String strBuzz = "Buzz";
		
		for (i = 1; i <= loopCount; i ++) {
			if (i % numFizz == 0 && i % numBuzz == 0) {
				System.out.println(strFizz + strBuzz);
				continue;
			} else if(i % numFizz == 0) {
				System.out.println(strFizz);
			} else if(i % numBuzz == 0) {
				System.out.println(strBuzz);
			} else {
				System.out.println(i);
			}
		}
		
	}
}